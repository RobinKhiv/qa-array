package com.qa.main;

public class Array {
	//Create a method that returns a boolean and accepts an integer as a parameter, 
	//if the integer is even, return true, if not then return false.
	public static boolean checkForEven(int i) {
		return i % 2 == 0 ? true : false;
	}
}
